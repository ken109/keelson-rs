// @generated by keelson-gen. DO NOT EDIT.
// Regenerate from the schema instead; hand-written code (hooks included)
// belongs outside this directory.

/// The model marker `users::table()` hangs off.
#[derive(Debug, Clone, Copy)]
pub struct Users;
/// One row of `users`.
#[derive(Debug, Clone, PartialEq)]
pub struct User {
    pub id: i32,
    pub name: String,
    pub email: Option<String>,
    pub age: Option<i32>,
    pub is_active: bool,
    pub created_at: chrono::DateTime<chrono::Utc>,
    /// Relations, filled by `preload`/`then_load` mods; empty otherwise.
    pub rel: Rel,
}
/// `users`' relations.
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Rel {
    /// Has-many `comments`, via `comments.user_id`.
    pub comments: Vec<super::comments::Comment>,
    /// Has-many `posts`, via `posts.user_id`.
    pub posts: Vec<super::posts::Post>,
}
impl keelson_exec::FromRow for User {
    fn from_row(row: &mut keelson_exec::Row) -> Result<Self, keelson_exec::ExecError> {
        Ok(User {
            id: row.take("id")?,
            name: row.take("name")?,
            email: row.take("email")?,
            age: row.take("age")?,
            is_active: row.take("is_active")?,
            created_at: row.take("created_at")?,
            rel: Rel::default(),
        })
    }
}
/// The three-state setter: unset fields stay out of the statement.
#[derive(Debug, Clone, Default)]
pub struct Setter {
    pub id: keelson_models::Set<i32>,
    pub name: keelson_models::Set<String>,
    pub email: keelson_models::Set<String>,
    pub age: keelson_models::Set<i32>,
    pub is_active: keelson_models::Set<bool>,
    pub created_at: keelson_models::Set<chrono::DateTime<chrono::Utc>>,
}
/// The entry point: `users::table().query(…)` / `.insert(…)` / ….
pub fn table() -> keelson_models::ModelTable<Users> {
    keelson_models::ModelTable::new()
}
pub fn id() -> keelson_models::Column<i32> {
    keelson_models::Column::new("users", "id")
}
pub fn name() -> keelson_models::Column<String> {
    keelson_models::Column::new("users", "name")
}
pub fn email() -> keelson_models::Column<String> {
    keelson_models::Column::new("users", "email")
}
pub fn age() -> keelson_models::Column<i32> {
    keelson_models::Column::new("users", "age")
}
pub fn is_active() -> keelson_models::Column<bool> {
    keelson_models::Column::new("users", "is_active")
}
pub fn created_at() -> keelson_models::Column<chrono::DateTime<chrono::Utc>> {
    keelson_models::Column::new("users", "created_at")
}
#[allow(clippy::type_complexity)]
fn all_columns() -> (
    keelson_models::Column<i32>,
    keelson_models::Column<String>,
    keelson_models::Column<String>,
    keelson_models::Column<i32>,
    keelson_models::Column<bool>,
    keelson_models::Column<chrono::DateTime<chrono::Utc>>,
) {
    (id(), name(), email(), age(), is_active(), created_at())
}
impl keelson_models::View for Users {
    type Row = User;
    type Select = keelson_psql::SelectQuery;
    fn base_select() -> Self::Select {
        keelson_psql::select((
            keelson_psql::select::columns(all_columns()),
            keelson_psql::select::from(keelson_psql::quote("users")),
        ))
    }
}
impl keelson_models::Table for Users {
    type Pk = i32;
    type Setter = Setter;
    type Insert = keelson_psql::InsertQuery;
    type Update = keelson_psql::UpdateQuery;
    type Delete = keelson_psql::DeleteQuery;
    fn insert_query(s: Setter) -> Self::Insert {
        let mut cols: Vec<&'static str> = Vec::new();
        let mut vals: Vec<keelson_core::expr::Expr> = Vec::new();
        s.id.push_into("id", &mut cols, &mut vals);
        s.name.push_into("name", &mut cols, &mut vals);
        s.email.push_into("email", &mut cols, &mut vals);
        s.age.push_into("age", &mut cols, &mut vals);
        s.is_active.push_into("is_active", &mut cols, &mut vals);
        s.created_at.push_into("created_at", &mut cols, &mut vals);
        let mut q = keelson_psql::insert((
            keelson_psql::insert::into(keelson_psql::quote("users")).columns(cols),
            keelson_psql::insert::returning(all_columns()),
        ));
        if !vals.is_empty() {
            q.apply(keelson_psql::insert::values(vals));
        }
        q
    }
    fn update_query() -> Self::Update {
        keelson_psql::update(keelson_psql::update::table(keelson_psql::quote("users")))
    }
    fn apply_setter(s: Setter, q: &mut Self::Update) {
        if let Some(v) = s.id.into_expr() {
            q.apply(keelson_psql::update::set_col("id").to(v));
        }
        if let Some(v) = s.name.into_expr() {
            q.apply(keelson_psql::update::set_col("name").to(v));
        }
        if let Some(v) = s.email.into_expr() {
            q.apply(keelson_psql::update::set_col("email").to(v));
        }
        if let Some(v) = s.age.into_expr() {
            q.apply(keelson_psql::update::set_col("age").to(v));
        }
        if let Some(v) = s.is_active.into_expr() {
            q.apply(keelson_psql::update::set_col("is_active").to(v));
        }
        if let Some(v) = s.created_at.into_expr() {
            q.apply(keelson_psql::update::set_col("created_at").to(v));
        }
    }
    fn delete_query() -> Self::Delete {
        keelson_psql::delete(keelson_psql::delete::from(keelson_psql::quote("users")))
    }
    fn pk(row: &User) -> Self::Pk {
        row.id
    }
    /// Delegates to the application's `crate::hooks::users::before_insert` (configured in `[tables.users] hooks`).
    fn before_insert<'a>(
        db: &'a dyn keelson_exec::Executor,
        setter: &'a mut Setter,
    ) -> keelson_exec::ExecFuture<'a, Result<(), keelson_exec::ExecError>> {
        crate::hooks::users::before_insert(db, setter)
    }
    /// Delegates to the application's `crate::hooks::users::after_insert` (configured in `[tables.users] hooks`).
    fn after_insert<'a>(
        db: &'a dyn keelson_exec::Executor,
        rows: &'a [User],
    ) -> keelson_exec::ExecFuture<'a, Result<(), keelson_exec::ExecError>> {
        crate::hooks::users::after_insert(db, rows)
    }
}
/// Then-load mods: a second query keyed by the first's rows.
pub mod then_load {
    /// Load each row's `comments` (to-many) with one keyed second query.
    pub fn comments() -> impl keelson_core::Mod<
        keelson_models::ModelSelect<super::Users>,
    > {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Users>| {
            q.add_loader(
                keelson_models::loader(|db, rows| { Box::pin(load_comments(db, rows)) }),
            );
        })
    }
    async fn load_comments(
        db: &dyn keelson_exec::Executor,
        rows: &mut [super::User],
    ) -> Result<(), keelson_exec::ExecError> {
        let mut keys: Vec<i32> = rows.iter().map(|r| r.id).collect();
        keys.sort_unstable();
        keys.dedup();
        if keys.is_empty() {
            return Ok(());
        }
        let related = super::super::comments::table()
            .query(super::super::comments::user_id().in_(keys))
            .all(db)
            .await?;
        keelson_models::attach_to_many(
            rows,
            related,
            |r| Some(r.id),
            |c| c.user_id,
            |r, cs| {
                r.rel.comments = cs;
            },
        );
        Ok(())
    }
    /// Load each row's `posts` (to-many) with one keyed second query.
    pub fn posts() -> impl keelson_core::Mod<keelson_models::ModelSelect<super::Users>> {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Users>| {
            q.add_loader(
                keelson_models::loader(|db, rows| { Box::pin(load_posts(db, rows)) }),
            );
        })
    }
    async fn load_posts(
        db: &dyn keelson_exec::Executor,
        rows: &mut [super::User],
    ) -> Result<(), keelson_exec::ExecError> {
        let mut keys: Vec<i32> = rows.iter().map(|r| r.id).collect();
        keys.sort_unstable();
        keys.dedup();
        if keys.is_empty() {
            return Ok(());
        }
        let related = super::super::posts::table()
            .query(super::super::posts::user_id().in_(keys))
            .all(db)
            .await?;
        keelson_models::attach_to_many(
            rows,
            related,
            |r| r.id,
            |c| c.user_id,
            |r, cs| {
                r.rel.posts = cs;
            },
        );
        Ok(())
    }
}
