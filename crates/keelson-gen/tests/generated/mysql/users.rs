// @generated by keelson-gen. DO NOT EDIT.
// Regenerate from the schema instead; hand-written code (hooks included)
// belongs outside this directory.

/// The model marker `users::table()` hangs off.
#[derive(Debug, Clone, Copy)]
pub struct Users;
/// One row of `users`.
#[derive(Debug, Clone, PartialEq)]
pub struct User {
    pub id: i32,
    pub name: String,
    pub email: Option<String>,
    pub age: Option<i32>,
    pub is_active: bool,
    pub created_at: chrono::NaiveDateTime,
    /// Relations, filled by `preload`/`then_load` mods; empty otherwise.
    pub rel: Rel,
}
/// `users`' relations.
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Rel {
    /// Has-many `comments`, via `comments.user_id`.
    pub comments: Vec<super::comments::Comment>,
    /// Has-many `post_authors`, via `post_authors.user_id`.
    pub post_authors: Vec<super::post_authors::PostAuthor>,
    /// Has-many `posts`, via `posts.user_id`.
    pub posts: Vec<super::posts::Post>,
    /// Has-one `user_emails`, via `user_emails.id`.
    pub user_emails: Option<Box<super::user_emails::UserEmail>>,
}
impl keelson_exec::FromRow for User {
    fn from_row(row: &mut keelson_exec::Row) -> Result<Self, keelson_exec::ExecError> {
        Ok(User {
            id: row.take("id")?,
            name: row.take("name")?,
            email: row.take("email")?,
            age: row.take("age")?,
            is_active: row.take("is_active")?,
            created_at: row.take("created_at")?,
            rel: Rel::default(),
        })
    }
}
/// The three-state setter: unset fields stay out of the statement.
#[derive(Debug, Clone, Default)]
pub struct Setter {
    pub id: keelson_models::Set<i32>,
    pub name: keelson_models::Set<String>,
    pub email: keelson_models::Set<String>,
    pub age: keelson_models::Set<i32>,
    pub is_active: keelson_models::Set<bool>,
    pub created_at: keelson_models::Set<chrono::NaiveDateTime>,
}
/// The entry point: `users::table().query(…)` / `.insert(…)` / ….
pub fn table() -> Users {
    Users
}
pub fn id() -> keelson_models::Column<i32> {
    keelson_models::Column::new("users", "id")
}
pub fn name() -> keelson_models::Column<String> {
    keelson_models::Column::new("users", "name")
}
pub fn email() -> keelson_models::Column<String> {
    keelson_models::Column::new("users", "email")
}
pub fn age() -> keelson_models::Column<i32> {
    keelson_models::Column::new("users", "age")
}
pub fn is_active() -> keelson_models::Column<bool> {
    keelson_models::Column::new("users", "is_active")
}
pub fn created_at() -> keelson_models::Column<chrono::NaiveDateTime> {
    keelson_models::Column::new("users", "created_at")
}
#[allow(clippy::type_complexity)]
fn all_columns() -> (
    keelson_models::Column<i32>,
    keelson_models::Column<String>,
    keelson_models::Column<String>,
    keelson_models::Column<i32>,
    keelson_models::Column<bool>,
    keelson_models::Column<chrono::NaiveDateTime>,
) {
    (id(), name(), email(), age(), is_active(), created_at())
}
impl keelson_models::View for Users {
    type Row = User;
    type Select = keelson_mysql::SelectQuery;
    fn base_select() -> Self::Select {
        keelson_mysql::select((
            keelson_mysql::select::columns(all_columns()),
            keelson_mysql::select::from(keelson_mysql::quote("users")),
        ))
    }
}
impl keelson_models::Table for Users {
    type Pk = i32;
    type Setter = Setter;
    type Insert = keelson_mysql::InsertQuery;
    type Update = keelson_mysql::UpdateQuery;
    type Delete = keelson_mysql::DeleteQuery;
    fn insert_query(s: Setter) -> Self::Insert {
        let mut cols: Vec<&'static str> = Vec::new();
        let mut vals: Vec<keelson_core::expr::Expr> = Vec::new();
        s.id.push_into("id", &mut cols, &mut vals);
        s.name.push_into("name", &mut cols, &mut vals);
        s.email.push_into("email", &mut cols, &mut vals);
        s.age.push_into("age", &mut cols, &mut vals);
        s.is_active.push_into("is_active", &mut cols, &mut vals);
        s.created_at.push_into("created_at", &mut cols, &mut vals);
        let mut q = keelson_mysql::insert(
            keelson_mysql::insert::into(keelson_mysql::quote("users")).columns(cols),
        );
        if !vals.is_empty() {
            q.apply(keelson_mysql::insert::values(vals));
        }
        q
    }
    fn update_query() -> Self::Update {
        keelson_mysql::update(
            keelson_mysql::update::table(keelson_mysql::quote("users")),
        )
    }
    fn apply_setter(s: Setter, q: &mut Self::Update) {
        if let Some(v) = s.id.into_expr() {
            q.apply(keelson_mysql::update::set_col("id").to(v));
        }
        if let Some(v) = s.name.into_expr() {
            q.apply(keelson_mysql::update::set_col("name").to(v));
        }
        if let Some(v) = s.email.into_expr() {
            q.apply(keelson_mysql::update::set_col("email").to(v));
        }
        if let Some(v) = s.age.into_expr() {
            q.apply(keelson_mysql::update::set_col("age").to(v));
        }
        if let Some(v) = s.is_active.into_expr() {
            q.apply(keelson_mysql::update::set_col("is_active").to(v));
        }
        if let Some(v) = s.created_at.into_expr() {
            q.apply(keelson_mysql::update::set_col("created_at").to(v));
        }
    }
    fn delete_query() -> Self::Delete {
        keelson_mysql::delete(keelson_mysql::delete::from(keelson_mysql::quote("users")))
    }
    fn pk(row: &User) -> Self::Pk {
        row.id
    }
    /// Delegates to the application's `crate::hooks::users::before_insert` (configured in `[tables.users] hooks`).
    fn before_insert<'a>(
        db: &'a dyn keelson_exec::Executor,
        setter: &'a mut Setter,
    ) -> keelson_exec::ExecFuture<'a, Result<(), keelson_exec::ExecError>> {
        crate::hooks::users::before_insert(db, setter)
    }
    /// Delegates to the application's `crate::hooks::users::after_insert` (configured in `[tables.users] hooks`).
    fn after_insert<'a>(
        db: &'a dyn keelson_exec::Executor,
        rows: &'a [User],
    ) -> keelson_exec::ExecFuture<'a, Result<(), keelson_exec::ExecError>> {
        crate::hooks::users::after_insert(db, rows)
    }
}
impl Users {
    /// A `SELECT` over this model — the dialect-generic path.
    pub fn query(
        self,
        mods: impl keelson_core::Mod<keelson_models::ModelSelect<Users>>,
    ) -> keelson_models::ModelSelect<Users> {
        keelson_models::ModelTable::<Users>::new().query(mods)
    }
    /// An `INSERT` of the setter's set fields, read back by key.
    pub fn insert(self, setter: Setter) -> Insert {
        Insert { setter, mods: Vec::new() }
    }
    /// An `UPDATE` of the setter's set fields — `exec` only.
    pub fn update(
        self,
        setter: Setter,
        mods: impl keelson_core::Mod<keelson_models::ModelUpdate<Users>>,
    ) -> Update {
        Update(keelson_models::ModelTable::<Users>::new().update(setter, mods))
    }
    /// A `DELETE` — `exec` only.
    pub fn delete(
        self,
        mods: impl keelson_core::Mod<keelson_models::ModelDelete<Users>>,
    ) -> Delete {
        Delete(keelson_models::ModelTable::<Users>::new().delete(mods))
    }
}
/// A pending `INSERT` on `users`: the setter, held unbuilt so `before_insert` can still rewrite it, plus the deferred Layer 1 mods.
pub struct Insert {
    setter: Setter,
    #[allow(clippy::type_complexity)]
    mods: Vec<Box<dyn FnOnce(&mut keelson_mysql::InsertQuery) + Send>>,
}
impl std::fmt::Debug for Insert {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("Insert")
            .field("setter", &self.setter)
            .field("mods", &self.mods.len())
            .finish()
    }
}
impl Insert {
    /// Defer Layer 1 mods onto the eventual statement.
    #[must_use]
    pub fn with(
        mut self,
        mods: impl keelson_core::Mod<keelson_mysql::InsertQuery> + Send + 'static,
    ) -> Self {
        self.mods.push(Box::new(move |q| mods.apply(q)));
        self
    }
    /// Insert, then read the row back by key. **Not** `RETURNING`: the two statements are not atomic — see the module's dialect notes.
    pub async fn one(
        self,
        db: &dyn keelson_exec::Executor,
    ) -> Result<User, keelson_exec::ExecError> {
        use keelson_exec::Execute as _;
        let Insert { mut setter, mods } = self;
        <Users as keelson_models::Table>::before_insert(db, &mut setter).await?;
        let k0 = match &setter.id {
            keelson_models::Set::Value(v) => Some(*v),
            _ => None,
        };
        let mut q = <Users as keelson_models::Table>::insert_query(setter);
        for m in mods {
            m(&mut q);
        }
        let done = q.execute(db).await?;
        let k0: i32 = k0
            .or_else(|| {
                done
                    .last_insert_id
                    .and_then(|id| {
                        <i32 as std::convert::TryFrom<i64>>::try_from(id).ok()
                    })
            })
            .ok_or_else(|| keelson_exec::ExecError::other(
                "users: the INSERT set no `id` and MySQL reported no last_insert_id, so the inserted row cannot be read back",
            ))?;
        let row: User = by_pk(k0).fetch_one(db).await?;
        <Users as keelson_models::Table>::after_insert(db, std::slice::from_ref(&row))
            .await?;
        Ok(row)
    }
    /// Insert for the side effect. `after_insert` still runs, with an empty row slice.
    pub async fn exec(
        self,
        db: &dyn keelson_exec::Executor,
    ) -> Result<keelson_exec::ExecResult, keelson_exec::ExecError> {
        use keelson_exec::Execute as _;
        let Insert { mut setter, mods } = self;
        <Users as keelson_models::Table>::before_insert(db, &mut setter).await?;
        let mut q = <Users as keelson_models::Table>::insert_query(setter);
        for m in mods {
            m(&mut q);
        }
        let done = q.execute(db).await?;
        <Users as keelson_models::Table>::after_insert(db, &[]).await?;
        Ok(done)
    }
}
/// The keyed read-back: `users`'s own columns, filtered by primary key. This is what stands in for `RETURNING` — a second statement, emitted as a function so its SQL is judged like any other.
pub fn by_pk(k0: i32) -> keelson_mysql::SelectQuery {
    keelson_mysql::select((
        keelson_mysql::select::columns(all_columns()),
        keelson_mysql::select::from(keelson_mysql::quote("users")),
        id().eq(k0),
    ))
}
/// A pending `UPDATE`. `exec` only: with no `RETURNING` there is nothing for an `all` verb to decode.
#[derive(Debug)]
pub struct Update(keelson_models::ModelUpdate<Users>);
impl Update {
    /// Apply mods written against the concrete statement.
    pub fn apply(&mut self, mods: impl keelson_core::Mod<keelson_mysql::UpdateQuery>) {
        self.0.apply(mods);
    }
    /// Update for the side effect; answers how many rows changed.
    pub async fn exec(
        self,
        db: &dyn keelson_exec::Executor,
    ) -> Result<keelson_exec::ExecResult, keelson_exec::ExecError> {
        self.0.exec(db).await
    }
}
/// A pending `DELETE`. `exec` only, for the same reason as `Update`.
#[derive(Debug)]
pub struct Delete(keelson_models::ModelDelete<Users>);
impl Delete {
    /// Apply mods written against the concrete statement.
    pub fn apply(&mut self, mods: impl keelson_core::Mod<keelson_mysql::DeleteQuery>) {
        self.0.apply(mods);
    }
    /// Delete for the side effect; answers how many rows went.
    pub async fn exec(
        self,
        db: &dyn keelson_exec::Executor,
    ) -> Result<keelson_exec::ExecResult, keelson_exec::ExecError> {
        self.0.exec(db).await
    }
}
/// Then-load mods: one keyed, batched query per level of a load path — `then_load::a().then(b::then_load::c())` is two levels, two queries, checked by the compiler.
pub mod then_load {
    /// Load each row's `comments` (to-many), one keyed query per batch of keys — `.then(…)` hangs the next level of the path off this one.
    pub fn comments() -> keelson_models::ThenLoad<
        super::Users,
        super::super::comments::Comments,
        i32,
    > {
        keelson_models::ThenLoad::new(keelson_models::Relation {
            parent_key: |r: &super::User| Some(r.id),
            child_key: |c: &super::super::comments::Comment| c.user_id,
            filter: |keys, q| keelson_core::Mod::apply(
                super::super::comments::user_id().in_(keys),
                q,
            ),
            attach: keelson_models::Attach::Many(|r: &mut super::User, cs| {
                r.rel.comments = cs;
            }),
        })
    }
    /// Load each row's `post_authors` (to-many), one keyed query per batch of keys — `.then(…)` hangs the next level of the path off this one.
    pub fn post_authors() -> keelson_models::ThenLoad<
        super::Users,
        super::super::post_authors::PostAuthors,
        i32,
    > {
        keelson_models::ThenLoad::new(keelson_models::Relation {
            parent_key: |r: &super::User| Some(r.id),
            child_key: |c: &super::super::post_authors::PostAuthor| Some(c.user_id),
            filter: |keys, q| keelson_core::Mod::apply(
                super::super::post_authors::user_id().in_(keys),
                q,
            ),
            attach: keelson_models::Attach::Many(|r: &mut super::User, cs| {
                r.rel.post_authors = cs;
            }),
        })
    }
    /// Load each row's `posts` (to-many), one keyed query per batch of keys — `.then(…)` hangs the next level of the path off this one.
    pub fn posts() -> keelson_models::ThenLoad<
        super::Users,
        super::super::posts::Posts,
        i32,
    > {
        keelson_models::ThenLoad::new(keelson_models::Relation {
            parent_key: |r: &super::User| Some(r.id),
            child_key: |c: &super::super::posts::Post| Some(c.user_id),
            filter: |keys, q| keelson_core::Mod::apply(
                super::super::posts::user_id().in_(keys),
                q,
            ),
            attach: keelson_models::Attach::Many(|r: &mut super::User, cs| {
                r.rel.posts = cs;
            }),
        })
    }
    /// Load each row's `user_emails` (to-one), one keyed query per batch of keys — `.then(…)` hangs the next level of the path off this one.
    pub fn user_emails() -> keelson_models::ThenLoad<
        super::Users,
        super::super::user_emails::UserEmails,
        i32,
    > {
        keelson_models::ThenLoad::new(keelson_models::Relation {
            parent_key: |r: &super::User| Some(r.id),
            child_key: |c: &super::super::user_emails::UserEmail| Some(c.id),
            filter: |keys, q| keelson_core::Mod::apply(
                super::super::user_emails::id().in_(keys),
                q,
            ),
            attach: keelson_models::Attach::One(|r: &mut super::User, c| {
                r.rel.user_emails = c.map(Box::new);
            }),
        })
    }
}
