// @generated by keelson-gen. DO NOT EDIT.
// Regenerate from the schema instead; hand-written code (hooks included)
// belongs outside this directory.

/// The model marker `comments::table()` hangs off.
#[derive(Debug, Clone, Copy)]
pub struct Comments;
/// One row of `comments`.
#[derive(Debug, Clone, PartialEq)]
pub struct Comment {
    pub id: i32,
    pub post_id: i32,
    pub user_id: Option<i32>,
    pub body: String,
    pub created_at: chrono::DateTime<chrono::Utc>,
    /// Relations, filled by `preload`/`then_load` mods; empty otherwise.
    pub rel: Rel,
}
/// `comments`' relations.
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Rel {
    /// Belongs-to `posts`, via `comments.post_id`.
    pub post: Option<super::posts::Post>,
    /// Belongs-to `users`, via `comments.user_id`.
    pub user: Option<super::users::User>,
}
impl keelson_exec::FromRow for Comment {
    fn from_row(row: &mut keelson_exec::Row) -> Result<Self, keelson_exec::ExecError> {
        Ok(Comment {
            id: row.take("id")?,
            post_id: row.take("post_id")?,
            user_id: row.take("user_id")?,
            body: row.take("body")?,
            created_at: row.take("created_at")?,
            rel: Rel::default(),
        })
    }
}
/// The three-state setter: unset fields stay out of the statement.
#[derive(Debug, Clone, Default)]
pub struct Setter {
    pub id: keelson_models::Set<i32>,
    pub post_id: keelson_models::Set<i32>,
    pub user_id: keelson_models::Set<i32>,
    pub body: keelson_models::Set<String>,
    pub created_at: keelson_models::Set<chrono::DateTime<chrono::Utc>>,
}
/// The entry point: `comments::table().query(…)` / `.insert(…)` / ….
pub fn table() -> keelson_models::ModelTable<Comments> {
    keelson_models::ModelTable::new()
}
pub fn id() -> keelson_models::Column<i32> {
    keelson_models::Column::new("comments", "id")
}
pub fn post_id() -> keelson_models::Column<i32> {
    keelson_models::Column::new("comments", "post_id")
}
pub fn user_id() -> keelson_models::Column<i32> {
    keelson_models::Column::new("comments", "user_id")
}
pub fn body() -> keelson_models::Column<String> {
    keelson_models::Column::new("comments", "body")
}
pub fn created_at() -> keelson_models::Column<chrono::DateTime<chrono::Utc>> {
    keelson_models::Column::new("comments", "created_at")
}
#[allow(clippy::type_complexity)]
fn all_columns() -> (
    keelson_models::Column<i32>,
    keelson_models::Column<i32>,
    keelson_models::Column<i32>,
    keelson_models::Column<String>,
    keelson_models::Column<chrono::DateTime<chrono::Utc>>,
) {
    (id(), post_id(), user_id(), body(), created_at())
}
impl keelson_models::View for Comments {
    type Row = Comment;
    type Select = keelson_psql::SelectQuery;
    fn base_select() -> Self::Select {
        keelson_psql::select((
            keelson_psql::select::columns(all_columns()),
            keelson_psql::select::from(keelson_psql::quote("comments")),
        ))
    }
}
impl keelson_models::Table for Comments {
    type Pk = i32;
    type Setter = Setter;
    type Insert = keelson_psql::InsertQuery;
    type Update = keelson_psql::UpdateQuery;
    type Delete = keelson_psql::DeleteQuery;
    fn insert_query(s: Setter) -> Self::Insert {
        let mut cols: Vec<&'static str> = Vec::new();
        let mut vals: Vec<keelson_core::expr::Expr> = Vec::new();
        s.id.push_into("id", &mut cols, &mut vals);
        s.post_id.push_into("post_id", &mut cols, &mut vals);
        s.user_id.push_into("user_id", &mut cols, &mut vals);
        s.body.push_into("body", &mut cols, &mut vals);
        s.created_at.push_into("created_at", &mut cols, &mut vals);
        let mut q = keelson_psql::insert((
            keelson_psql::insert::into(keelson_psql::quote("comments")).columns(cols),
            keelson_psql::insert::returning(all_columns()),
        ));
        if !vals.is_empty() {
            q.apply(keelson_psql::insert::values(vals));
        }
        q
    }
    fn update_query() -> Self::Update {
        keelson_psql::update(
            keelson_psql::update::table(keelson_psql::quote("comments")),
        )
    }
    fn apply_setter(s: Setter, q: &mut Self::Update) {
        if let Some(v) = s.id.into_expr() {
            q.apply(keelson_psql::update::set_col("id").to(v));
        }
        if let Some(v) = s.post_id.into_expr() {
            q.apply(keelson_psql::update::set_col("post_id").to(v));
        }
        if let Some(v) = s.user_id.into_expr() {
            q.apply(keelson_psql::update::set_col("user_id").to(v));
        }
        if let Some(v) = s.body.into_expr() {
            q.apply(keelson_psql::update::set_col("body").to(v));
        }
        if let Some(v) = s.created_at.into_expr() {
            q.apply(keelson_psql::update::set_col("created_at").to(v));
        }
    }
    fn delete_query() -> Self::Delete {
        keelson_psql::delete(keelson_psql::delete::from(keelson_psql::quote("comments")))
    }
    fn pk(row: &Comment) -> Self::Pk {
        row.id
    }
}
/// Preload mods: the relation joins into the *same* query.
pub mod preload {
    /// Same-query `LEFT JOIN` preload of the to-one `post`.
    pub fn post() -> impl keelson_core::Mod<
        keelson_models::ModelSelect<super::Comments>,
    > {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Comments>| {
            use keelson_psql::Chain as _;
            use keelson_psql::Mod as _;
            (
                keelson_psql::select::left_join(keelson_psql::quote("posts"))
                    .on(
                        keelson_psql::quote(("posts", "id"))
                            .eq(keelson_psql::quote(("comments", "post_id"))),
                    ),
                keelson_psql::select::preload_columns((
                    keelson_psql::quote(("posts", "id")).as_("post.id"),
                    keelson_psql::quote(("posts", "user_id")).as_("post.user_id"),
                    keelson_psql::quote(("posts", "title")).as_("post.title"),
                    keelson_psql::quote(("posts", "status")).as_("post.status"),
                    keelson_psql::quote(("posts", "views")).as_("post.views"),
                    keelson_psql::quote(("posts", "published_at"))
                        .as_("post.published_at"),
                )),
            )
                .apply(q);
            q.add_mapper_mod(
                keelson_models::mapper_mod(|row, parent: &mut super::Comment| {
                    parent.rel.post = post_from_preload(row)?;
                    Ok(())
                }),
            );
        })
    }
    /// Decode the prefixed columns; the joined key column decides a `LEFT JOIN` miss.
    pub fn post_from_preload(
        row: &mut keelson_exec::Row,
    ) -> Result<Option<super::super::posts::Post>, keelson_exec::ExecError> {
        if matches!(row.value("post.id"), None | Some(keelson_psql::Value::Null)) {
            return Ok(None);
        }
        Ok(
            Some(super::super::posts::Post {
                id: row.take("post.id")?,
                user_id: row.take("post.user_id")?,
                title: row.take("post.title")?,
                status: row.take("post.status")?,
                views: row.take("post.views")?,
                published_at: row.take("post.published_at")?,
                rel: super::super::posts::Rel::default(),
            }),
        )
    }
    /// Same-query `LEFT JOIN` preload of the to-one `user`.
    pub fn user() -> impl keelson_core::Mod<
        keelson_models::ModelSelect<super::Comments>,
    > {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Comments>| {
            use keelson_psql::Chain as _;
            use keelson_psql::Mod as _;
            (
                keelson_psql::select::left_join(keelson_psql::quote("users"))
                    .on(
                        keelson_psql::quote(("users", "id"))
                            .eq(keelson_psql::quote(("comments", "user_id"))),
                    ),
                keelson_psql::select::preload_columns((
                    keelson_psql::quote(("users", "id")).as_("user.id"),
                    keelson_psql::quote(("users", "name")).as_("user.name"),
                    keelson_psql::quote(("users", "email")).as_("user.email"),
                    keelson_psql::quote(("users", "age")).as_("user.age"),
                    keelson_psql::quote(("users", "is_active")).as_("user.is_active"),
                    keelson_psql::quote(("users", "created_at")).as_("user.created_at"),
                )),
            )
                .apply(q);
            q.add_mapper_mod(
                keelson_models::mapper_mod(|row, parent: &mut super::Comment| {
                    parent.rel.user = user_from_preload(row)?;
                    Ok(())
                }),
            );
        })
    }
    /// Decode the prefixed columns; the joined key column decides a `LEFT JOIN` miss.
    pub fn user_from_preload(
        row: &mut keelson_exec::Row,
    ) -> Result<Option<super::super::users::User>, keelson_exec::ExecError> {
        if matches!(row.value("user.id"), None | Some(keelson_psql::Value::Null)) {
            return Ok(None);
        }
        Ok(
            Some(super::super::users::User {
                id: row.take("user.id")?,
                name: row.take("user.name")?,
                email: row.take("user.email")?,
                age: row.take("user.age")?,
                is_active: row.take("user.is_active")?,
                created_at: row.take("user.created_at")?,
                rel: super::super::users::Rel::default(),
            }),
        )
    }
}
/// Then-load mods: a second query keyed by the first's rows.
pub mod then_load {
    /// Load each row's `post` (to-one) with one keyed second query.
    pub fn post() -> impl keelson_core::Mod<
        keelson_models::ModelSelect<super::Comments>,
    > {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Comments>| {
            q.add_loader(
                keelson_models::loader(|db, rows| { Box::pin(load_post(db, rows)) }),
            );
        })
    }
    async fn load_post(
        db: &dyn keelson_exec::Executor,
        rows: &mut [super::Comment],
    ) -> Result<(), keelson_exec::ExecError> {
        let mut keys: Vec<i32> = rows.iter().map(|r| r.post_id).collect();
        keys.sort_unstable();
        keys.dedup();
        if keys.is_empty() {
            return Ok(());
        }
        let related = super::super::posts::table()
            .query(super::super::posts::id().in_(keys))
            .all(db)
            .await?;
        keelson_models::attach_to_one(
            rows,
            related,
            |r| r.post_id,
            |c| c.id,
            |r, c| {
                r.rel.post = c;
            },
        );
        Ok(())
    }
    /// Load each row's `user` (to-one) with one keyed second query.
    pub fn user() -> impl keelson_core::Mod<
        keelson_models::ModelSelect<super::Comments>,
    > {
        keelson_core::mod_fn(|q: &mut keelson_models::ModelSelect<super::Comments>| {
            q.add_loader(
                keelson_models::loader(|db, rows| { Box::pin(load_user(db, rows)) }),
            );
        })
    }
    async fn load_user(
        db: &dyn keelson_exec::Executor,
        rows: &mut [super::Comment],
    ) -> Result<(), keelson_exec::ExecError> {
        let mut keys: Vec<i32> = rows.iter().filter_map(|r| r.user_id).collect();
        keys.sort_unstable();
        keys.dedup();
        if keys.is_empty() {
            return Ok(());
        }
        let related = super::super::users::table()
            .query(super::super::users::id().in_(keys))
            .all(db)
            .await?;
        keelson_models::attach_to_one(
            rows,
            related,
            |r| r.user_id,
            |c| Some(c.id),
            |r, c| {
                r.rel.user = c;
            },
        );
        Ok(())
    }
}
